function hook(target){
    return document.getElementById(target);
}
// get elemenets from dom
const one = hook(one),
    two = hook(two),
    three = hook(three),
    four = hook(four),
    five = hook(five),
    six = hook(six),
    seven = hook(seven),
    eight = hook(eight),
    nine = hook(nine),
    zero = hook(zero),
    plus = hook(plus),
    subt = hook(subt)
// add event listeners